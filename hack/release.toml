# commit to be tagged for the new release
commit = "HEAD"

project_name = "talos-vmtoolsd"
github_repo = "siderolabs/talos-vmtoolsd"
match_deps = "^github.com/(siderolabs/[a-zA-Z0-9-]+)$"

# previous = -
# pre_release = true

# [notes]
